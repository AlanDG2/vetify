# Automation Project

Automation repository for Playwright UI and API tests.

## Prerequisites

- [Node.js](https://nodejs.org/) 18 or newer
- npm

## Getting started

```bash
cp .env.example .env
npm install
npx playwright install
npm setup
npm test
```

## Commands

| Command                   | Description                                             |
| ------------------------- | ------------------------------------------------------- |
| `npm run setup`           | Prepare fresh test users from setup tags                |
| `npm test`                | Run all Playwright projects                             |
| `npm run test:unit`       | Unit tests for internal helpers                         |
| `npm run test:ui`         | Playwright UI mode                                      |
| `npm run test:headed`     | Run with visible browser                                |
| `npm run report`          | Open HTML report from last run                          |
| `npm run allure:generate` | Generate Allure report in `allure-report/`              |
| `npm run allure:report`   | Generate report, enforce quality gate, then open report |

## Project layout

- `package.json` — npm scripts and dependencies
- `playwright.config.ts` — Playwright configuration
- `tsconfig.json` — TypeScript configuration
- `documentation/` — project documentation and development standards
- `playwright/` — Playwright support code and project-level test assets
- `allure-results/` — raw Allure execution results
- `allure-report/` — generated Allure HTML report
- `playwright-report/` — latest Playwright HTML report and assets
- `test-results/` — saved test-run artifacts (snapshots, traces, reports)
- `src/` and `tests/` follow this structure:

```text
src/
│
├── api/
│   ├── vetify/
│   │   ├── institutional/
│   │   └── webapp/
│   ├── ike/
│   └── auth0/
│
├── config/
│
├── fixtures/
│
├── helpers/
│
├── integrations/
│   ├── mercadopago/
│   ├── salesforce/
│   └── ...
│
├── pages/
│   ├── vetify/
│   │   ├── institutional/
│   │   └── webapp/
│   └── ike/
│
├── report/
│   └── allure/
│
├── scripts/
│
└── types/

tests/
│
├── setup/
├── projects/
│   ├── flux-capitado/
│   ├── osde-adquirente/
│   ├── osde-capitado/
│   ├── vetify-b2c/
│   └── webapp/
└── .../
```

## Notes

- The default `.env.example` values are placeholders — replace them with target Dev/QA/Prod URLs and credentials.
- Use `npm run report` to open the HTML report generated by the last run (served from `playwright-report/index.html`).

## Test user generation

> [!Note]
> Since the creation process takes aproximatelly 10min we group all the requests for new test users
> and create them first and start the tests when they are available

## Setup script (`npm run setup`)

This script prepares user-related test preconditions before the main test run.

It is responsible for:

- Scanning Playwright tests in list mode (no execution) to collect setup tags.
- Translating `@New...` tags into concrete fresh-user creation requests.
- Creating requested users before tests start, including plan count suffixes like `+3`.
- Waiting after successful creation so downstream data (plans) is available to tests.

Operational behavior:

- `IGNORE_USER_CREATION=true` skips user creation and finishes setup immediately.
- If test listing or tag parsing fails, setup continues with an empty tag list.
- If user-provisioning APIs are unavailable, setup logs the failure and continues.
- The script only waits when new users were actually created.

### Tags

Tag format rules:

- Base tag (without `User`) creates a user without post-creation registration.
- `...User` tag creates a user that must be registered post creation.
- `+{X}` suffix sets how many plans are purchased (examples: `+1`, `+3`, `+8`).

Supported user creation tags:

- **Vetify B2C**
    - `@NewVetifyB2C`
    - `@NewVetifyB2C+{X}`
    - `@NewVetifyB2CUser`
    - `@NewVetifyB2CUser+{X}`
- **Adquirente OSDE**
    - `@NewAdquirenteOSDE`
    - `@NewAdquirenteOSDE+{X}`
    - `@NewAdquirenteOSDEUser`
    - `@NewAdquirenteOSDEUser+{X}`
- **Capitado OSDE**
    - `@NewCapitadoOSDE`
    - `@NewCapitadoOSDEUser`
- **Capitado Flux**
    - `@NewCapitadoFlux`
    - `@NewCapitadoFluxUser`
