# Backlog de automatización — gap analysis vs. Casos de Prueba manuales

> Qué falta automatizar, priorizado por esfuerzo/valor. Fuente: `documentation/Casos de Prueba.xlsx` (columna `Automatizado?`) cruzado contra el código real (`tests/projects/**/*.spec.ts`, POMs en `src/pages/`). Complementa `docs/coverage-register.md` (que trackea lo YA automatizado por HU) — este archivo trackea lo que falta, No confundir ambos.
>
> **Última actualización**: 2026-08-07 — Tier 2 (Videollamadas) queda 100% cerrado, ver `docs/coverage-register.md` para las 6 HUs de videollamada. **2026-08-31**: Gestión de Usuario TS-04 (Olvidé contraseña) desbloqueada y cerrada en los 3 productos que tenían el gap — ver sección "Bloqueado" más abajo. **2026-09-06**: Tier 3 (abajo) queda cerrado — confirmado que Gestión de Usuario TS-03 ya existe y pasa en los 4 productos, no era backlog real. TS-05 (Cambio de contraseña) construido de punta a punta para OSDE Adquirente (IMAS-3218) — código completo, bloqueado por `BUG-034` (compra real falla). Ver `docs/lecciones-aprendidas.md` y `documentation/casos-de-prueba-trazabilidad.md` (entrada 2026-09-06) para el detalle completo de la sesión de estabilización. **2026-09-07**: al auditar `Automation Vetify.xlsx` en busca de gaps reales, se confirmó en vivo que `tests/projects/vetify-webapp/session.spec.ts` (Logout, persistencia de sesión, ruta protegida sin auth) y `navigation.spec.ts` (Navegación principal, mapa de veterinarias — acceso y búsqueda —, permisos por plan) **ya estaban 100% construidos y pasando** (10/10 y 5/6 tests verdes) pese a figurar como gaps de 0% en el roadmap — no era backlog real, era desactualización del tracker ejecutivo. Único hallazgo real de esta pasada: una regresión nueva en el Tour de onboarding "primer login" (ver sección "Bloqueado").

## Nota — hojas `Reintegros` y `Miscelaneas` — CORREGIDA 2026-09-10, la nota anterior estaba obsoleta

Esta nota decía que ambas hojas estaban "vacías, 0 casos diseñados" y que Reintegros necesitaba VPN "sin acceso al momento de este análisis" — **ninguna de las 2 afirmaciones es cierta hoy** (la VPN se destrabó el 2026-08-22, ver IMP-013 resuelto; y releer el Excel directamente hoy mostró que ambas hojas siempre tuvieron contenido real, la nota nunca se actualizó tras escribirlo):

- **`Miscelaneas`**: 9 CPs diseñados y con `Automatizado? = Sí` (mobile Appium/WebdriverIO, "sin equivalente Desktop") — Mascotas, Planes, Servicios, Ayuda, Historial de atención, Vetify PLUS, Asistencia a domicilio, Facturas. No es un gap, está prácticamente cerrado.
- **`Reintegros`**: 6 CPs (no 0). CP-01 (cuentas de acreditación) y CP-06 (consulta de historial) ya estaban `Automatizable = Sí`/verificados en vivo. CP-02 (Solicitud de reintegro completa) y CP-03 (Carga de documentación) quedaban en "DBD" citando un bloqueo (`IMAS-4279`) que ya estaba **arreglado desde el 2026-09-02** — se resolvieron hoy con evidencia real ya existente en `docs/impedimentos-bloqueos.md` (IMP-013): ambos ahora `Automatizable = Sí`, con Pasos/Resultado Esperado reales, listos para que alguien escriba el `.spec.ts`. CP-04/CP-05 (validación de campos, datos inválidos) siguen en DBD — **se intentó la sesión de exploración en vivo el mismo día y quedó bloqueada de entrada**: `GET /api/bff/reintegros/mascotas` devolvió `200 []` para 2 cuentas Capitado reales distintas (una de ellas con reintegros reales visibles segundos antes en la pantalla anterior), bloqueando el wizard en el primer paso ("No hay mascotas registradas"). No es el mismo bug que bloqueaba antes (`IMAS-4279`, ya resuelto) — es una recurrencia nueva de `IMP-016`, reconfirmada con evidencia fresca el mismo día que otros 4 endpoints de la misma familia de outage (`IMP-017`). Retomar cuando el ambiente esté sano — ver `docs/impedimentos-bloqueos.md`.

**Nota 2026-08-07, sin confirmar si es el mismo sistema**: se accedió sin problema (vía MCP, sin necesidad aparente de VPN — aunque no se puede descartar que la VPN ya estuviera activa en la máquina del usuario) a `reintegros-backoffice.ike.qa` (certificado propio, requiere `--ignore-https-errors` en `.mcp.json`) durante IMAS-3742. Es un panel de gestión de reintegros/reembolsos veterinarios (solicitudes "VETI-...", expedientes SISE) — no está confirmado si es el mismo backoffice que `institucional.ike.qa` mencionado arriba o uno distinto. Si resulta ser el mismo, revisar si el bloqueo de VPN sigue vigente.

## Tier 1 — infraestructura ya construida, esfuerzo bajo

Todo sobre `tests/projects/vetify-webapp/credentials.spec.ts` + `VetifyWebappAddPetFormPage` (`src/pages/vetify/webapp/credentials/AddPetFormPage.ts`), ya usados por specs verdes.

| Hoja / ID | Título | Crítico | Estado en código | Nota |
|---|---|---|---|---|
| ~~Credenciales CP-21 (TS-02 Paso 5)~~ | Subir foto — Archivo en formato no permitido | No | **✅ Automatizado 2026-08-07** — `credentials.spec.ts` TC-21, verde en Desktop+Android. Mensaje real confirmado: "No se pudo cargar la foto", "Continuar" queda deshabilitado |
| ~~Credenciales CP-02 (TS-03 Crear Credencial)~~ | Cargar credencial sin foto (Omitir) | **Sí** | **No aplica — investigado 2026-08-07**: no existe ningún botón "Omitir" en la pantalla real de carga de foto. "Continuar" queda deshabilitado tanto antes de subir nada como después de un intento fallido — la foto es obligatoria hoy en la UI, a diferencia de lo que asume el caso diseñado en el Excel. No se automatiza (no tiene sentido codificar un flujo que no existe); no se reporta como bug (no está confirmado si es un cambio de requisito intencional) |
| Credenciales CP-18/CP-19/CP-20 (TS-02 Paso 5) | Cámara no disponible / disponible / tomar foto | No | Stubs vacíos con `test.skip('Test not implemented yet.')` | Requiere flags de Playwright (`--use-fake-device-for-media-stream --use-fake-ui-for-media-stream`), no bloqueado externamente |

## Tier 2 — mismo spec ya verde, extensiones incrementales

`tests/projects/vetify-webapp/videocall.spec.ts` (IMAS-3889, multi-mascota) está 100% DoD — estos son casos adicionales sobre `VideocallFormPage`, ya construido y probado.

| Hoja / ID | Título | Crítico | Nota |
|---|---|---|---|
| ~~Videollamadas CP06~~ | [Negativo] "otro motivo" vuelve obligatorio el comentario adicional | No | **✅ Automatizado 2026-08-07** — `videocall.spec.ts` TC-10 (TS-03 IMAS-3889), verde en Desktop+Android. Confirmado en vivo: al elegir "Otro motivo" el campo se marca "Obligatorio" y bloquea "Continuar" |
| ~~Videollamadas CP09~~ | Botón "Continuar" en adjuntos deshabilitado sin archivo | No | **✅ Automatizado 2026-08-07** — cubierto indirectamente por TC-02/TC-09 (**IMAS-4023**) |
| ~~Videollamadas CP10~~ | [Negativo] Rechazo de formato de archivo no permitido | No | **✅ Automatizado 2026-08-07** (**IMAS-4023** CA01/CA02/CA06) — `videocall.spec.ts` TC-07, texto real confirmado en vivo: "No se pudo subir el archivo. Intentá nuevamente." |
| ~~Videollamadas CP12~~ | [Borde] Bloqueo de subir al llegar a 5 archivos | No | **✅ Automatizado 2026-08-07** (**IMAS-4023** CA01/CA05) — TC-07. El input de archivo se oculta al llegar a 5 (no el bloque de texto, que sigue visible) |
| ~~Videollamadas CP13~~ | [Borde, mobile] Bloqueo de cámara al llegar a 5 archivos | No | **✅ Automatizado 2026-08-07** (**IMAS-4023**) — TC-08, corrido y verificado en Android real |
| ~~Videollamadas CP15~~ | [Negativo] [API] Bypass de validaciones de archivos directo al backend | No | **No aplica — reevaluado 2026-08-07**: confirmado en vivo que el front llama al mismo endpoint (`POST /api/files/upload/pets`) para CUALQUIER archivo, válido o no; no hay un gate solo-frontend que "bypasear". La validación real ya se ejercita en TC-07 (CP10) |
| Videollamadas CA03 (IMAS-4023) | Reintento de carga tras error (archivo inválido → válido) | Sí | **✅ Automatizado 2026-08-07** — `videocall.spec.ts` TC-09 |
| ~~Videollamadas CA07 (IMAS-4023)~~ | Analítica de intentos/rechazos/reintentos de carga | No | **Descartado 2026-08-07** — confirmado con la PO (Pau): no fue algo hablado al definir la HU y no aporta valor suficiente, queda fuera de alcance. No automatizar |

## Tier 2b — Reseteo de contraseña (IMAS-3215), parcialmente destrabado

`tests/projects/vetify-b2c/user-management.spec.ts` (TS-04) — 5/18 CPs automatizados 2026-08-07. Ver `docs/user-stories/IMAS-3215-reseteo-contrasena-b2c-vetify.tests.md`.

| CP | Título | Estado |
|---|---|---|
| ~~CP01-CP05~~ | Acceso al flujo + solicitud de reset (registrado/no registrado/vacío/formato inválido) | **✅ Automatizado 2026-08-07**, 5/5 verdes |
| CP06-CP12 | Recepción/contenido del email, link de reset, redirección | 🔴 Bloqueado — IMP-006 (sin infra de lectura de casilla). Confirmado que `/api/passrecovery` no expone el token en la respuesta |
| CP13-CP18 | Cambiar contraseña, confirmar, loguearse con la nueva, vieja invalidada | 🔴 Bloqueado — depende de tener un link/token de reset válido (mismo motivo que arriba) |

**Para destrabar CP06-CP18 sin infraestructura de email**: preguntarle al equipo de dev si puede exponerse el token/link de reset de otra forma en QA (endpoint de debug, feature flag, etc.). Sin eso, solo queda esperar a que se resuelva IMP-006 (IMAS-3467, lectura de casilla de correo).

**IMAS-3216 (Capitado OSDE) / IMAS-3217 (Capitado FLUX)**: mismo bloqueo aplica 1:1 — ambos reusan la misma pantalla `/auth/login` de Vetify webapp (confirmado en `docs/user-stories/IMAS-3216-*.md` / `IMAS-3217-*.md`), sin diseño de casos nuevo necesario.

## Tier 3 — ~~alcance nuevo~~ CERRADO 2026-09-06, no era backlog real

~~Gestión de Usuario TS-03 CP-01/CP-02~~ — confirmado en vivo el 2026-09-06 que **ya existen y pasan** en los 4 productos (Vetify B2C, OSDE Adquirente, OSDE Capitado, Flux Capitado) — `IMP-002` no aplicó, la pantalla de activación es la misma en los 4. De paso se encontró y arregló un bug real en CP-02 (el test leía el `<select>` de tipos de documento antes de que la API respondiera — `expect.poll` en vez de lectura directa, ver `docs/lecciones-aprendidas.md`).

## Bloqueado — no es por VPN, son impedimentos ya documentados

No avanzar acá aunque no dependan de la VPN de Reintegros:

- **Gestión de Usuario TS-04** (Olvidé contraseña, fase de solicitud, los 4 productos) — ✅ **ya no bloqueado, 100% automatizado y verde desde 2026-08-31** (ver `docs/coverage-register.md`, IMAS-3215/3216/3217). `IMP-006` (sin infraestructura de lectura de casilla de correo) se resolvió el 2026-08-28 con `EmailClient` (IMAP real).
- ~~**Gestión de Usuario TS-05** (Cambio de contraseña vía link de email)~~ — ✅ **resuelto 2026-09-01**, ya no bloqueado en ningún producto. La falla del 2026-08-31 no era rate-limit del backend: era un bug real en `EmailClient.ts` (`client.mailbox.uidNext` nunca se refrescaba). Con el fix + 2 cuentas Gmail reales nuevas para OSDE Capitado/Flux Capitado, los 3 productos (Vetify B2C, OSDE Capitado, Flux Capitado) tienen TS-05 construido y verde. Ver `docs/coverage-register.md` (IMAS-3215/3216/3217).
- **Perfil — "Cambiar DNI - Nuevo DNI ya registrado con otro usuario"** y **"...Nuevo DNI sin utilizar en el sistema"** — Pasos/Resultado esperado siguen como `DBD` (por definir) en el Excel, no están listos para automatizar todavía (falta definición, no es un bloqueo técnico).
- **Flujo de Compra** (las 8 variantes de compra exitosa + fallida, por producto) — no bloqueado en el Excel, pero cruza con `IMP-004` (vinculación compra↔usuario poco confiable en QA, requiere esperar propagación) — evaluar caso por caso antes de comprometerse a un tier.
- ~~**Flujo de Compra — "Aplicar un cupón válido"**~~ — **CERRADO 2026-09-10**: `VETIFY20X3` (el mismo cupón ya usado en `checkout-footer-cupon.spec.ts` solo para verificar el footer) se confirmó real y funcional contra el checkout completo de Vetify B2C — aplicado sobre el plan 2319 (Vetify Emergencias, $19.990), descontó genuinamente "20% OFF los primeros 3 meses" (subtotal sin cambios, total con el descuento real). Escrito y verificado en vivo: `tests/projects/vetify-b2c/purchase-flow.spec.ts` TS-04 Cupones TC-02, 2/2 verde junto con TC-01. Nota aparte, sin investigar más: las bases y condiciones muestran que la promo vence el 01/03/2026 (ya pasado) pero el descuento se aplica igual — posible copy desactualizado o falta de validación de fecha en el backend.
- ~~**Reintegros CP-02/CP-03 "backlog libre, sin impedimento"**~~ — **CORRECCIÓN 2026-09-10, el mismo día**: se había dicho que estos 2 CPs ya no tenían ningún bloqueo (solo faltaba escribir el `.spec.ts`) porque `IMAS-4279` estaba resuelto. Al ir a escribir el código real, se reconfirmó `IMP-016` con la misma cuenta: el endpoint de mascotas ya no da `[]` pero ahora da un registro con `mascotaId`/`nombre` en `null` — mismo bloqueo práctico, la UI sigue sin mostrar ninguna mascota utilizable. **Estos 2 CPs SÍ están bloqueados por IMP-016**, igual que CP-04/CP-05 — no se escribió el camino feliz para evitar inventar selectores nunca vistos funcionando en esta sesión. Ver `docs/impedimentos-bloqueos.md`.
- **Flujo de Compra — cupón, resto de las variantes — BLOQUEADO 2026-09-10 (IMP-029), no por falta de intento**: se escribieron y se intentaron correr en vivo "Eliminar el cupón agregado" y "Agregar cupón en paso 1" (mismo patrón que el TC-02 ya verde) — ambos quedaron colgados porque el checkout institucional empezó a bloquear la carga de la landing (pantalla en blanco, solo reCAPTCHA) tras ~2 interacciones reales seguidas en la misma sesión de test. Confirmado 2 veces (corrida en paralelo y serial). El código quedó escrito y marcado `test.skip` con referencia a IMP-029 (`tests/projects/vetify-b2c/purchase-flow.spec.ts`) — listo para reactivar apenas se resuelva el impedimento. "Cambiar el cupón agregado" tiene un bloqueo adicional: necesita un segundo código de cupón real distinto de `VETIFY20X3`, que no existe en el repo. **"Compra exitosa con cupón" tiene DOS capas de bloqueo, no una**: además de IMP-029, el endpoint de pago (`pagar-mp`) está inestable hoy de nuevo (IMP-012 reabierto, ver `docs/bugs/BUG-035`) — resolver solo IMP-029 no alcanza para esas variantes puntuales. Ver `docs/impedimentos-bloqueos.md` IMP-029/IMP-012.
- **Flujo de Compra — re-auditoría general, 2026-09-06**: `purchase-flow.spec.ts` de Vetify B2C corrido completo hoy — 7/20 fallas, **las 7 son comportamiento errático de MercadoPago sandbox** (tarjetas "aprobadas" rechazadas, una "rechazada" aprobada, un motivo de rechazo equivocado), no bugs de test ni de producto. Documentado en `docs/bugs/BUG-035`. No se corrieron los otros 3 productos (mismo bloqueo esperable). Retomar cuando el ambiente se estabilice.
- **Gestión de Usuario TS-05 — OSDE Adquirente (IMAS-3218)**: construido de punta a punta el 2026-09-06 (4 tests, mismo patrón que los otros 3 productos) — código completo, typecheck/lint limpio, pero **no verificado en vivo**: bloqueado por `BUG-034` (la compra real de OSDE Adquirente falla con `500 internal_error`, no se puede dar de alta la cuenta `REAL_EMAIL` necesaria). Retomar apenas se resuelva BUG-034 — no hace falta escribir nada más, solo correr y verificar.
- ~~**Gestión de Usuario TS-03 CP-03/CP-04/CP-05 (Documento no existente/ya registrado/Activación exitosa) — pool Fresh/`UNREGISTERED` se agota y no se repone solo**~~ — **REPUESTO 2026-09-10**: confirmado 2026-09-06 en los 4 productos que estos 3 CPs se skipeaban por "No available fresh user... tags [UNREGISTERED]" — el lote inicial de cuentas fresh se consume corrida tras corrida (quedan `ACTIVE` tras activarse) y nadie generaba cuentas `UNREGISTERED` nuevas automáticamente. No era un bug de código, solo un pendiente operativo: se corrió `UserFactory.generateTestUsers({registration:false})` para los 4 productos (`src/scripts/generate-fresh-unregistered-adquirente.ts` + `-capitado.ts`) — 1 cuenta `UNREGISTERED` nueva por producto, sin necesidad de esperar los 15 min de propagación de IMP-004 (no se activan a propósito, quedan sin registrar). Costo real: 1 token `TESTOSDE*` + 1 `TESTFLUX*` consumidos del pool de IMP-001 (quedan 7 + 8). **Intento de corrida en vivo el mismo día (Vetify B2C, TC-02/TC-03)**: ya NO se skipea por falta de usuario (el pool funcionó) — pero falla igual, esta vez por el outage activo de IMP-017 (`/validation/policy` → `502`, mensajes de página nunca aparecen, timeout de 60s). Es decir: el bloqueo de datos quedó resuelto hoy, pero la verificación en vivo sigue sin poder completarse mientras dure el outage — no cuenta como 100% todavía, ver IMP-017.
- ~~Condicionado del plan (`plans.spec.ts` TS-01) — bug real~~ — **CERRADO 2026-09-10, era falso positivo del test, no un bug**: lo que el 2026-09-07 parecía un botón roto (`BUG-036`) resultó ser que `newPage.url()` nunca se actualiza para una pestaña de PDF embebido — el botón real funciona bien (confirmado con click manual y vía MCP). Test corregido para verificar por la respuesta de red del contexto en vez de `Page.url()`. 2/2 verde. Ver retractación completa en `docs/bugs/BUG-036`.
- **Prestadores/Red (QA-AUTO-056/057/058 del roadmap) — explorado en vivo 2026-09-07, no son backlog real**: la funcionalidad real de "Buscar veterinaria" hoy es solo un mapa de Google centrado en la dirección buscada — no hay lista de veterinarias reales cercanas ni tarjeta de detalle de prestador (QA-AUTO-056 no aplica, la pantalla no existe todavía). El buscador usa Google Places real, que casi nunca devuelve cero sugerencias incluso con texto sin sentido — no hay un estado de "sin resultados" confiable para gatillar sin mockear la API (QA-AUTO-057). Se confirmaron los 15 items reales del menú lateral (`SideMenuSection`) y ninguno corresponde a "Atención de Red" como funcionalidad propia — probablemente se solapaba con Emergencias/Asistencia/Historial, ya cubiertos (QA-AUTO-058). Ningún hallazgo nuevo aplicable, solo corrección de notas del roadmap.
- **Cambio/selección de mascota (QA-AUTO-037 del roadmap) — explorado en vivo 2026-09-07, no es backlog real**: no existe un concepto de "mascota activa" a nivel de sesión en la webapp — cada pantalla lista todas las mascotas como tarjetas individuales; el único selector real de "elegir para cuál mascota es esto" vive dentro del flujo de Videollamada, ya cubierto por sus propios CPs. De paso se reconfirmó `BUG-033` en vivo (la misma cuenta pasó de 2 mascotas reales a 0 en menos de 1 minuto entre 2 corridas) — sigue activo, no bloqueó esta exploración puntual pero impide confirmar visualmente el listado completo de mascotas por ahora.
- **Videollamadas — pool `VETIFY_ADQUIRENTE` vacío para `WITH_PET+NO_EMPTY_PLAN` (1 plan)**: confirmado 2026-09-06 que había **cero** cuentas reales con esta combinación (no "pool chico", vacío) — bloqueaba ~8 bloques de `videocall.spec.ts`. Corregido retageando 1 cuenta que ya tenía mascota real pero tags desactualizados (`pooled-users.json`); el framework libera la reserva después de cada test, así que 1 cuenta sana alcanza para los 8 bloques corriendo en secuencia. Verificado que destraba el skip — pero el test en sí quedó bloqueado de nuevo por el mismo outage intermitente de `BUG-033` (reconfirmado ese mismo día en esta cuenta). Reintentar cuando el ambiente esté sano.
- **Tour de onboarding "primer login" (`session.spec.ts` TS-03 TC-01) — regresión real confirmada 2026-09-07**: la migración a `UserSource.Fresh` del 2026-09-04 (pensada para resolver agotamiento de pool) hace que la propia activación de la cuenta Fresh "gaste" el único primer-login que este test necesita — el test falla siempre (2/2, no flaky) desde entonces. Ver `docs/lecciones-aprendidas.md` entrada 2026-09-07 y `docs/coverage-register.md`. Requiere decisión de arquitectura (pool Fresh dedicado sin cacheo de sesión, o volver a `Pooled` con cuenta virgen real) antes de poder arreglarlo — no es un fix de una línea.
- ~~Cámara en carga de credencial~~ — **CERRADO 2026-09-07, reabierto y resuelto técnicamente 2026-09-10 (IMP-019)**: `TS-03 Crear Credencial` TC-02 automatiza la subida real vía "Usar cámara" (mismo endpoint que la subida de archivo, `POST /api/files/upload/pets`), verde y verificado. El CP original "cámara no disponible → deshabilitado" (CP-18) se había dado por no automatizable el 2026-09-07 (visibilidad dependiente del hardware real de la máquina) — **corregido 2026-09-10**: se implementó exactamente la solución que en ese momento se descartó por "no estar configurada", sobreescribiendo `navigator.mediaDevices.enumerateDevices()` vía `page.addInitScript()` para fijar de forma determinística el estado "disponible"/"no disponible" (TC-03/TC-04 nuevos). Código y typecheck listos; **pendiente de verificar en vivo** (bloqueado por un outage de ambiente el mismo día que se escribió). De paso se encontró y limpió una cuenta pool con decenas de tags `ERROR` acumulados que bloqueaba repetidamente la selección (`fresh-users.json`, no versionado). Ver `docs/lecciones-aprendidas.md` 2026-09-07 para el detalle completo de la investigación original.

## Descartado permanentemente — sacado del código 2026-09-08, no volver a escribir

Estos 4 CPs vivían como `test.skip(true, ...)` en el código, apareciendo como "omitido" en cada corrida de Allure para siempre, sin ningún camino a que algún día pasen a verde (a diferencia de la sección "Bloqueado" de arriba, que sí puede destrabarse). Se sacaron del código para dejar de inflar el conteo de omitidos con algo que nunca va a cambiar — el motivo de cada uno queda acá.

- **Credenciales CP-02 "Cargar credencial sin foto (Omitir)"** (`credentials.spec.ts`, TS-03): la foto es obligatoria por diseño al crear la credencial de una mascota — el paso 5 nunca tuvo un botón "Omitir". Confirmado con negocio 2026-08-07. El CP del Excel quedó desactualizado, no es un bug ni un gap.
- **Videollamadas IMAS-3894 CA05 "Habilitación de 'Ingresar' dentro de la ventana de 5 minutos"** (`videocall.spec.ts`, TS-05): el caso positivo requeriría un turno agendado dentro de los próximos 5 minutos, lo cual choca con la anticipación mínima de 30 minutos que exige el propio wizard de agendamiento — solo alcanzable esperando en tiempo real sobre un turno ya agendado, no viable en un test automatizado sin volverlo lento/flaky. Validado manualmente contra QA real (vía MCP): con un turno dentro de la ventana, "Ingresar" se habilita correctamente. El caso negativo (deshabilitado fuera de ventana) sí sigue automatizado en TC-01 de la misma suite.
- **Videollamadas IMAS-3894 CA06-CA08 "Sala de espera, ingreso y salida de la videoconsulta"** (`videocall.spec.ts`, TS-05): requiere una sesión de videoconferencia real con un profesional conectado del otro lado — no reproducible desde QA automatizado sin un simulador de proveedor de videollamada, que no existe en este proyecto.
- **Videollamadas IMAS-3894 CA09 "Comunicaciones"** (`videocall.spec.ts`, TS-05): no hay forma de verificar el envío real de email/push desde el pipeline automatizado — mismo criterio ya aplicado a IMAS-4023 CA07 (analítica, descartado 2026-08-07).

**Se mantienen en el código** (bloqueo temporal, con cuentas reales algún día se destraban — no confundir con la lista de arriba): los 5 CPs de `access-control.spec.ts` (IMAS-3742), bloqueados por `IMP-005` (no hay usuarios de prueba con cuenta real en el sistema de identidad de Iké).

## Ver también

- `docs/coverage-register.md` — inventario de lo YA automatizado por HU (estado real, ambiente).
- `docs/impedimentos-bloqueos.md` — detalle completo de IMP-002, IMP-004, IMP-006.
- `qa-workspace/known-issues.md` — issues de ambiente/pool de usuarios, no de diseño de casos.
